# macOS
.DS_Store

# Claude tooling
.claude/

# Raw photography source (6.2GB — too large for Git, kept locally)
Dutchman's Pipe Club Photography/

# Original design handoff bundle (regenerable from claude.ai/design)
Dutchman's Pipe Club Website-handoff.zip

# Backup of original Unsplash placeholders (kept locally for revert only)
assets/placeholders-original/

# Reference videos from the client (kept locally, not for the repo)
Screen Recording*.mov
hf_*.mp4

# JSX build toolchain (compiled .js are committed; deps are not)
node_modules/
package-lock.json

# Internal working handoff doc (Claude session continuity — not for the client repo)
HANDOFF.md
